const createDispute = (state, action) => {
  switch (action.type) {
    case 'ADD_DISPUTE':
      return
    default:

  }
}

const byid = (state = {}, action) => {
  switch (action.type) {
    case 'ADD_DISPUTE':

      break;
    default:

  }
}
